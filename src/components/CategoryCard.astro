---
import MediaFrame from './MediaFrame.astro';
import type { PublicMedia } from '../data/public-catalog';

interface Props {
  title: string;
  description: string;
  href: string;
  detail: string;
  media: PublicMedia;
  imageAlt: string;
}

const { title, description, href, detail, media, imageAlt } = Astro.props;
---
<a class="category-card" href={href}>
  <MediaFrame
    media={media}
    alt={imageAlt}
    class="category-card__media"
    sizes="(max-width: 760px) 100vw, (max-width: 1100px) 50vw, 36vw"
  />
  <div class="category-card__content">
    <p class="category-card__count">{detail}</p>
    <h3>{title}</h3>
    <p>{description}</p>
    <span class="text-link">Explore {title.toLowerCase()} <span aria-hidden="true">&rarr;</span></span>
  </div>
</a>
